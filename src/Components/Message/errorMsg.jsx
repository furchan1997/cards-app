export const ErrorMessage = ({ message }) => {
  return <div className="container">{message}</div>; // מציג את הודעת השגיאה בתוך אלמנט div עם מחלקת container
};

export default ErrorMessage; // ייצוא רכיב
